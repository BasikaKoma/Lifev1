VITE_SUPABASE_URL=https://fxdnbepmiphyzebqdkyf.supabase.co
# anon PUBLIC key from Supabase → Project Settings → API (NOT service_role)
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
VITE_OPENAI_API_KEY=your-openai-key-here
# Public URL for project invite links (e.g. https://lifev1.com or http://localhost:5173)
VITE_APP_URL=

# Supabase Edge Function secrets (Dashboard → Edge Functions → Secrets — NOT in Vite)
# OPENAI_API_KEY=   # used by brain-run (web/Android). Desktop stores the key in OS safeStorage.
# OURA_CLIENT_ID=
# OURA_CLIENT_SECRET=
# OURA_REDIRECT_URI=https://fxdnbepmiphyzebqdkyf.supabase.co/functions/v1/oura-oauth-callback
# OURA_SUCCESS_REDIRECT=http://127.0.0.1:17823/?oura=connected
# META_APP_ID=
# META_APP_SECRET=
# META_REDIRECT_URI=https://fxdnbepmiphyzebqdkyf.supabase.co/functions/v1/meta-oauth-callback
# META_SUCCESS_REDIRECT=http://127.0.0.1:17823/?meta=connected
# Meta Developer Console: Business app + Facebook Login for Business + Instagram.
# Add the redirect URI above as a Valid OAuth Redirect URI. Development testers only until App Review.
# Publish needs pages_manage_posts + instagram_content_publish enabled on the Meta app, then reconnect.

# Desktop auto-update publish (local only — never commit or ship in the app)
# GitHub repo that hosts installers (Supabase free tier max upload is 50 MB)
GITHUB_OWNER=your-github-username
GITHUB_REPO=lifev1
GH_TOKEN=ghp_your_personal_access_token
